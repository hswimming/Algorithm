class Solution {
    public int solution(int num1, int num2) {
        int answer = 0;
        double total = 0.0;
        
        total = (double)num1 / (double)num2 * 1000;
        
        return answer += total;
    }
}